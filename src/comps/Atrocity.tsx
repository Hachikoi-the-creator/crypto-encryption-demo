import { runCompleteExample } from "@/utils/walletFuncs";

export default function Atrocity() {
  return (
    <div>
      <button onClick={() => runCompleteExample()}>
        Click me for testing some cool stuff, trust you wan tt click this lil
        cute silly baka button
      </button>
    </div>
  );
}
